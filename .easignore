# Dependencies / caches
node_modules
.pnpm-store
.expo
.git
.vercel
.vscode
.DS_Store
*.log
dist
tmp
.next
out
*.tsbuildinfo
build*/
*.ipa

# Secrets — never upload to EAS (.easignore replaces .gitignore entirely)
.env
.env.*
.env.local
.env.*.local
**/.env
**/.env.*
**/.env.local

# Archives / backups
_archive_mobile
backup
Backups*
**/backup
**/backups
**/Backups*

# Web / server code not needed for iOS mobile build
apps/web
apps/orders
api
supabase

# Native / build caches
android/.gradle
android/app/build